# Unit tests

Pure-function tests compiled via `typst compile` with `assert.eq` checks.

Covers: error message grammar, step markers, content traversal, content reconstruction, container registry, sequence splitting, the wrappers Quarto's Typst writer emits.
